version: "3.8"

services:
  singlefile-webservice:
    build: .
    container_name: singlefile-webservice
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      - PORT=8080
      - TIMEOUT=60
